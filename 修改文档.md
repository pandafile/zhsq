# 社区后台系统修改文档

## 概述

本次修改涉及 5 个文件的改动，涵盖 2 个主要功能模块（人员管理 `hxPerson`、房屋管理 `hxRoom`），主要完成以下内容：

1. **房屋地址三级级联选择器**（人员管理 → 搜索区 + 新增/修改弹窗）
2. **小区/楼栋二级级联选择器**（房屋管理 → 搜索区 + 新增/修改弹窗）
3. **房屋地址列显示**（人员管理 → 列表 + 详情）
4. **JSON 字段 el-tag 展示**（人员管理 → 列表）
5. **出生日期去时分秒**
6. **民意平台同步增强**

---

## 修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `src/views/hx/hxPerson/list/index.vue` | 搜索区级联、表格列展示、JSON 字段、日期格式 |
| `src/views/hx/hxPerson/list/component/edit.vue` | 编辑弹窗级联 |
| `src/views/hx/hxPerson/list/component/detail.vue` | 详情页地址显示 |
| `src/views/hx/hxRoom/list/index.vue` | 搜索区级联 |
| `src/views/hx/hxRoom/list/component/edit.vue` | 编辑弹窗级联 |
| `src/api/hx/minyi.ts` | 民意同步字段映射 |

---

## 修改一：房屋地址三级级联（人员管理）

### 1.1 搜索区 `index.vue`

**原代码**（房屋ID 下拉框）：
```vue
<el-col :span="8" :class="showAll ? 'colBlock' : 'colNone'">
  <el-form-item label="房屋ID" prop="roomId">
    <el-select filterable v-model="tableData.param.roomId" placeholder="请选择房屋ID" clearable style="width:200px;">
      <el-option
        v-for="item in roomIdOptions"
        :key="item.key"
        :label="item.value"
        :value="item.key"
      />
    </el-select>
  </el-form-item>
</el-col>
```

**新代码**（房屋地址三级级联 → 小区 → 楼栋 → 房间）：
```vue
<el-col :span="8" :class="showAll ? 'colBlock' : 'colNone'">
  <el-form-item label="房屋地址">
    <el-cascader
      v-model="tableData.param.roomId"
      :props="{ label:'label', value:'id', children:'children', lazy: true, lazyLoad: cascadeLoad, checkStrictly: true, emitPath: false }"
      placeholder="请选择房屋地址"
      filterable
      clearable
      style="width:200px;"
    />
  </el-form-item>
</el-col>
```

**关键参数说明**：
- `lazy: true` + `lazyLoad: cascadeLoad` — 懒加载模式，按需加载数据
- `checkStrictly: true` — 允许选中任意一级
- `emitPath: false` — 只返回选中节点的值（房间 ID）

### 1.2 新增懒加载方法 `cascadeLoad`

在 `index.vue` 的 `<script setup>` 中新增（放在 `linkedData` 函数之后）：

```typescript
const cascadeLoad = (node: any, resolve: (nodes: any[]) => void) => {
  if (node.level === 0) {
    // 加载小区
    listHxCommunity({ pageSize: 9999 }).then((res: any) => {
      const communities = res?.data?.list ?? []
      resolve(communities.map((c: any) => ({
        id: `community_${c.id}`,
        label: c.communityName,
        isLeaf: false,
        _communityId: c.id,
      })))
    }).catch(() => resolve([]))
  } else if (node.level === 1) {
    // 加载楼栋
    const communityId = node.data._communityId
    listHxBuilding({ pageSize: 9999, communityId }).then((res: any) => {
      const buildings = res?.data?.list ?? []
      resolve(buildings.map((b: any) => ({
        id: `building_${b.id}`,
        label: b.buildingName,
        isLeaf: false,
        _buildingId: b.id,
      })))
    }).catch(() => resolve([]))
  } else if (node.level === 2) {
    // 加载房间
    const buildingId = node.data._buildingId
    listHxRoom({ pageSize: 9999, buildingId }).then((res: any) => {
      const rooms = res?.data?.list ?? []
      resolve(rooms.map((r: any) => ({
        id: r.id,
        label: r.roomNo,
        isLeaf: true,
      })))
    }).catch(() => resolve([]))
  } else {
    resolve([])
  }
}
```

### 1.3 新增 API 导入

在 import 区域加入：
```typescript
import { listHxCommunity } from "/@/api/hx/hxCommunity";
import { listHxBuilding } from "/@/api/hx/hxBuilding";
import { listHxRoom } from "/@/api/hx/hxRoom";
```

### 1.4 删除废弃变量

删除 `addressOptions` 相关的接口和变量声明（懒加载不需要预填 `options`）：
```typescript
// 删除以下代码
// interface AddressNode { ... }
// const addressOptions = ref<AddressNode[]>([])
```

### 1.5 编辑弹窗 `edit.vue`（同步修改）

**原代码**：
```vue
<el-form-item label="房屋ID" prop="roomId">
  <el-select filterable clearable v-model="formData.roomId" placeholder="请选择房屋ID">
    <el-option v-for="item in roomIdOptions" :key="item.key" :label="item.value" :value="item.key" />
  </el-select>
</el-form-item>
```

**新代码**：
```vue
<el-form-item label="房屋地址" prop="roomId">
  <el-cascader
    v-model="formData.roomId"
    :props="{ label:'label', value:'id', children:'children', lazy: true, lazyLoad: cascadeLoad, checkStrictly: true, emitPath: false }"
    placeholder="请选择房屋地址"
    filterable
    clearable
    style="width:200px;"
  />
</el-form-item>
```

同样需要：
1. 新增导入 `listHxCommunity`、`listHxBuilding`、`listHxRoom`
2. 新增 `cascadeLoad` 方法（与 `index.vue` 相同）

---

## 修改二：小区/楼栋二级级联（房屋管理）

### 2.1 搜索区 `hxRoom/list/index.vue`

**原代码**：
```vue
<el-col :span="8" class="colBlock">
  <el-form-item label="楼栋" prop="BuildingId">
    <el-cascader
      v-model="tableData.param.BuildingId"
      placeholder="请选择楼栋"
      :options="buildingIdOptions"
      filterable
      clearable
      :props="{ label: 'buildingName',value: 'id',checkStrictly: true,emitPath: false }"
    />
  </el-form-item>
</el-col>
```

**新代码**：
```vue
<el-col :span="8" class="colBlock">
  <el-form-item label="小区/楼栋" prop="BuildingId">
    <el-cascader
      v-model="tableData.param.BuildingId"
      placeholder="请选择小区和楼栋"
      :props="{ label:'label', value:'id', children:'children', lazy: true, lazyLoad: cascadeLoad, checkStrictly: true, emitPath: false }"
      filterable
      clearable
    />
  </el-form-item>
</el-col>
```

### 2.2 新增导入和 `cascadeLoad`

```typescript
import { listHxBuilding } from "/@/api/hx/hxBuilding";
import { listHxCommunity } from "/@/api/hx/hxCommunity";
```

```typescript
const cascadeLoad = (node: any, resolve: (nodes: any[]) => void) => {
  if (node.level === 0) {
    listHxCommunity({ pageSize: 9999 }).then((res: any) => {
      const communities = res?.data?.list ?? []
      resolve(communities.map((c: any) => ({
        id: `community_${c.id}`,
        label: c.communityName,
        isLeaf: false,
        _communityId: c.id,
      })))
    }).catch(() => resolve([]))
  } else if (node.level === 1) {
    const communityId = node.data._communityId
    listHxBuilding({ pageSize: 9999, communityId }).then((res: any) => {
      const buildings = res?.data?.list ?? []
      resolve(buildings.map((b: any) => ({
        id: b.id,
        label: b.buildingName,
        isLeaf: true,
      })))
    }).catch(() => resolve([]))
  } else {
    resolve([])
  }
}
```

**注意**：房屋管理是二级级联（小区 → 楼栋），选中楼栋时 `id` 直接使用楼栋 ID（无需 `building_` 前缀），因为后端接口的 `BuildingId` 参数就是楼栋 ID。

### 2.3 编辑弹窗 `hxRoom/list/component/edit.vue`

同样将"楼栋ID"改为"小区/楼栋"，使用懒加载 `el-cascader`，并添加 `cascadeLoad` 方法（与上述相同）。

---

## 修改三：房屋地址列显示

### 3.1 列表 `index.vue`

**原代码**：
```vue
<el-table-column label="房屋ID" align="center" prop="linkedRoomId.roomNo" min-width="150px" />
```

**新代码**：
```vue
<el-table-column label="房屋地址" align="center" min-width="150px">
  <template #default="scope">
    <span>{{ [scope.row.linkedRoomId?.buildingName, scope.row.linkedRoomId?.roomNo].filter(Boolean).join('') || '-' }}</span>
  </template>
</el-table-column>
```

### 3.2 详情 `detail.vue`

**原代码**：
```vue
{{ formData.linkedRoomId?formData.linkedRoomId.roomNo:'' }}
```

**新代码**：
```vue
{{ [formData.linkedRoomId?.buildingName, formData.linkedRoomId?.roomNo].filter(Boolean).join('') || '-' }}
```

---

## 修改四：JSON 字段 el-tag 展示（人员管理列表）

### 4.1 删除展开行，改为 el-tag 方式

将原来 5 个 JSON 字段（基础信息、特殊标签、健康信息、家庭信息、社保信息）的展开行方式删除，改为 `el-tag` 展示：

```vue
<el-table-column label="基础信息" align="left" prop="basicInfo" min-width="200px">
  <template #default="scope">
    <div v-if="scope.row.basicInfo">
      <el-tag
        v-for="(value, key) in parseJson(scope.row.basicInfo)"
        :key="key"
        size="small"
        style="margin: 2px"
      >{{ value }}</el-tag>
    </div>
    <span v-else>-</span>
  </template>
</el-table-column>
```

其余 4 个字段（特殊标签、健康信息、家庭信息、社保信息）同理。

### 4.2 新增 parseJson 方法

```typescript
const parseJson = (str: string): Record<string, any> => {
  if (!str) return {};
  try {
    const obj = typeof str === 'string' ? JSON.parse(str) : str;
    return typeof obj === 'object' ? obj : {};
  } catch {
    return {};
  }
};
```

---

## 修改五：出生日期去时分秒

### 5.1 列表显示 `index.vue`

```typescript
// 改前
proxy.parseTime(scope.row.birthday, '{y}-{m}-{d} {h}:{i}:{s}')
// 改后
proxy.parseTime(scope.row.birthday, '{y}-{m}-{d}')
```

### 5.2 详情显示 `detail.vue`

```typescript
// 改前
proxy.parseTime(formData.birthday, '{y}-{m}-{d} {h}:{i}:{s}')
// 改后
proxy.parseTime(formData.birthday, '{y}-{m}-{d}')
```

### 5.3 搜索日期选择器

```vue
<!-- 改前 -->
<el-date-picker type="datetime" format="YYYY-MM-DD HH:mm:ss" value-format="YYYY-MM-DD HH:mm:ss" />

<!-- 改后 -->
<el-date-picker type="date" format="YYYY-MM-DD" value-format="YYYY-MM-DD" />
```

### 5.4 编辑日期选择器

同理，`type` 从 `datetime` 改为 `date`，去掉时间格式。

---

## 修改六：民意平台同步增强

### 6.1 补充字段详情 `minyi.ts`

在 `handleViewUpdate` 中，用 `pageQueryList` 按 `petitionNumber` 逐一补充详细字段：

```typescript
// 在过滤新记录之后、逐条插入之前，补充详细字段
for (const item of platformList) {
  try {
    const detailRes = await pageQueryList({
      currentPage: 1,
      pageSize: 10,
      petitionLocationAddress: '',
      petitionClassification: '',
      petitionTagLabels: [],
      petitionFlagLabels: [],
      petitionStatusLabels: [],
      petitionProcessLabels: [],
      petitionNumber: item.petitionNumber,
      petitionContent: null,
      nextOrgId: '',
      areaCode: '',
      overdueStatus: '',
      overdueType: '',
      reportFlag: '',
      areaDataSwitch: '1'
    }, token.accessToken)
    const detail = detailRes?.data?.records?.[0]
    if (detail) {
      // 把 pageQuery 可能缺失的字段补上
      item.petitionTitle = item.petitionTitle || detail.petitionTitle
      item.petitionContent = item.petitionContent || detail.petitionContent
      item.petitionSource = item.petitionSource || detail.petitionSource
      // ... 其他字段同理
    }
    // 留白字段，供手动填写
    item._communityId = undefined
    item._handlePerson = undefined
  } catch (e) {
    console.warn('补充详情失败，跳过:', item.petitionNumber, e)
  }
}
```

### 6.2 petitionStatusToNumber 支持数字码

```typescript
function petitionStatusToNumber(status: string): number | undefined {
  // 如果已经是数字码（'1'~'5'），直接转数字
  if (/^[1-5]$/.test(status)) {
    return Number(status)
  }
  // 中文名映射
  const map: Record<string, number> = {
    '待办理': 1,
    '办理中': 2,
    '已办结': 3,
    '已超期': 4,
    '已退回': 5,
  }
  return map[status]
}
```

### 6.3 字段容错

```typescript
appealCategory: p.suggestionCategory || p.petitionClassification || '其他',
```

---

## 如何验证修改

1. **人员管理 → 搜索**：点击"房屋地址"级联选择器，应依次展开 小区 → 楼栋 → 房间
2. **人员管理 → 新增/修改**：房屋地址级联同搜索区
3. **人员管理 → 列表**：房屋地址列显示"楼栋名称+房号"
4. **人员管理 → 列表**：JSON 字段（基础信息/特殊标签等）显示为 el-tag 标签
5. **房屋管理 → 搜索**：小区/楼栋级联选择器
6. **房屋管理 → 新增/修改**：小区/楼栋级联同搜索区
7. **出生日期**：不再显示 `00:00:00`
8. **民意同步**：补充字段后同步成功

---

## 可能遇到的问题

| 问题 | 原因 | 解决 |
|------|------|------|
| 级联不展开 | `lazyLoad` 未正确绑定 | 检查 `:props` 中是否有 `lazy: true, lazyLoad: cascadeLoad` |
| 楼栋显示为空 | `communityId` 参数名不对 | 确认后端接口接收的字段名（有些是 `communityId` 有些是 `community_id`） |
| 级联卡顿 | 数据量太大 | 调整 `pageSize` 或改为搜索时触发加载 |
